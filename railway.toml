[deploy]
startCommand = "./start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 60

[environments.production.variables]
MR_TRANSPORT = "sse"
MR_HOST = "0.0.0.0"
MR_DB_PATH = "/data/meeting_rooms.db"
PYTHONUNBUFFERED = "1"
